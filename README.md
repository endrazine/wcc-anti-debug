## WCC anti-debug Tests

This repository contains artifacts to test wsh's resistance against anti-debug techniques.

### How to build and run

This repository has been tested on Ubuntu 24.04 LTS and assumes WCC is already pre-installed.

To build:
	make

Then, to run tests:
	make test
